version: "3"
services:
  config-front:
    image: registry.cn-beijing.aliyuncs.com/codeforfun/config-server-frontend:1.0.2
    build: .
    container_name: config-front
    ports:
      - "80:80"
networks:
  default:
    external:
      name: local
